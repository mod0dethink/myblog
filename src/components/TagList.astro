---
const { tags = [], prefix = 'tags' } = Astro.props;
const base = import.meta.env.BASE_URL;
---

{tags.length ? (
  <div class="tag-list">
    {tags.map((tag) => (
      <a class="tag" href={`${base}${prefix}/${encodeURIComponent(tag)}/`}>{tag}</a>
    ))}
  </div>
) : null}
